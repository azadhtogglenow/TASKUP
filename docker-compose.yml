version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: pg-performance
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: shop
    ports:
      - "5432:5432"
    volumes:
      - ./init:/docker-entrypoint-initdb.d